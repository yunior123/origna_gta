name: CI — Mobile (Disabled)

# Firebase Test Lab has been removed — backend is now OrignaBase on VPS.
# Mobile integration tests run via strict-quality-audit.yml (Flutter on Linux + xvfb).
# This workflow is intentionally a no-op.

on:
  pull_request:
    branches: ["main"]

jobs:
  skip:
    name: Mobile CI (disabled — Firebase Test Lab removed)
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: echo "Mobile Test Lab CI disabled. Firebase infrastructure removed."
